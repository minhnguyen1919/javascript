    // var grid = [["0,0", "1,0", "2,0"],
    //        ["0,1", "1,1", "2,1"]];
    // Get element in two direction array
    // row = 1, col = 2           
    // grid[1][2];

    // // Use single array instead of 2 arrays
    // // x,y can be found by getting the element at position x + y * width in the array, where width is the width of the grid.
    // var grid = ["0,0", "1,0", "2,0",
    // "0,1", "1,1", "2,1"];  
    // grid[2 + 1 * 3];